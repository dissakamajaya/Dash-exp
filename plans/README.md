# Implementation Plans

Generated by the improve skill on 2026-07-23, audit stamp commit `85e6c6d`.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan  | Title                                          | Priority | Effort | Depends on       | Status |
|-------|------------------------------------------------|----------|--------|------------------|--------|
| 001   | Cut app entrance duration to 400ms             | HIGH     | S      | —                | DONE   |
| 002   | Add tap feedback to ShapeGrid icon buttons     | MEDIUM   | S      | —                | DONE   |
| 003   | Switch AnimatePresence to popLayout            | MEDIUM   | S      | —                | DONE   |
| 004   | Replace `transition-all` on password input     | LOW-MED  | S      | —                | DONE   |
| 005   | Extract shared motion tokens                   | LOW-MED  | S      | 001              | DONE   |
| 006   | Add GitHub Actions CI pipeline                 | P1       | S      | —                | DONE   |
| 007   | Refactor ShapeGrid to key-by-id shape registry | P1       | M      | —                | DONE   |
| 008   | Render coming-soon splash without session      | P1       | S      | —                | DONE   |
| 009   | Update AGENTS.md to current architecture       | P2       | S      | —                | DONE   |
| 010   | Add tests for worker/access.ts                 | P1       | M      | —                | DONE   |
| 011   | Routing tests + remove dead SELECTOR_ITEMS     | P2       | M      | —                | DONE   |
| 012   | Add .mcp.json to .gitignore                    | P3       | S      | —                | DONE   |
| 013   | Runtime shapeIndex uniqueness check            | P3       | S      | (superseded by 007) | SUPERSEDED |
| 014   | Drop @paper-design/shaders-react              | P3       | M      | direction, confirm with maintainer | TODO |
| 015   | Default gateway to dark theme                  | P3       | S      | direction, confirm with maintainer | TODO |
| 016   | Persist selection only for authenticated users | P3       | S      | —                | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED | SUPERSEDED

## Dependency notes

- **Plan 006 (CI)** should land first because plans 007, 010, 011 all add
  tests, and CI provides the safety net for those changes.
- **Plan 007 (shape registry)** and **Plan 013 (shape index guard)** are
  alternative approaches to the same problem. If 007 lands first, mark
  013 as SUPERSEDED. If 013 lands first, plan 007 still works (string IDs
  are a stronger check).
- **Plan 014 (drop Paper shaders)** is independent but visually disruptive.
  Land 007 + 008 first so the visual baseline is stable, then 014 in its
  own commit for easy revert.
- **Plan 015 (default dark)** is a one-line UX-policy change. Land
  independently of everything else.
- **Plan 016 (gated persistence)** is a privacy improvement with no
  dependencies.

## Recommended execution batches

- **Batch 1 (foundational safety)**: 006 → 010 → 011
- **Batch 2 (correctness + docs)**: 008 → 009 → 012
- **Batch 3 (refactor)**: 007 → 013 (or skip 013) → 011 cleanup
- **Batch 4 (direction, optional)**: 014 → 015 → 016

Each batch can ship independently as a single PR.

## Findings considered and rejected

- **Framer Motion shorthand `y`/`scale` → full `transform` string** (LOW).
  Would require touching every `motion.div`/`motion.button` in the app.
  Effort/reward not justified at this codebase size — flagging for awareness
  only. (Already noted in original animation plans README.)
- **Per-icon stagger on grid mount** (missed opportunity). Worth doing as
  a *separate* plan once the high/mid-severity items land; out of scope.
- **Glow filter animation** on `ShapeGrid.tsx:255-261` (the drop-shadow
  snaps while sibling props ease). LOW polish; no plan written.
- **Spatial icon → confirm-panel connection** (missed opportunity). Requires
  layout-id bridging; not a quick win.
- **Replace `window.location.assign` with `window.open`** — already done
  in earlier session; not a planning concern.

## Recon facts (locked in at audit time, commit `85e6c6d`)

- React 19 + Vite 7 + TypeScript 5.9 + Tailwind 4 + Motion 12 + cuelume + jose
- Worker: Cloudflare Workers via wrangler 4
- Package manager: Bun
- Test runner: Vitest (`bunx vitest run`)
- Typecheck: `bunx tsc --noEmit` (1 pre-existing warning: `worker/local-auth.ts:73`)
- Build: `bun run build` (singlefile plugin, ~880KB output)
- Deployment: `bun run deploy` (build + wrangler deploy)
- Source of truth for destinations: `src/data/gateway.ts` (7 destinations, 3 staff)
- All plans numbered 006+ to avoid conflicting with the animation plans 001-005
  that already shipped.
